'use strict';

// number - число
const a = 10;

// string - строка
const str = 'Hello';

// boolean - булев тип
const flag = true;

// undefined
let b;

// null
let myName = null;

if (flag) {
    console.log(`number = ${a}`);
    console.log(`string = ${str}`);
    console.log(`undefined = ${b}`);
    console.log(`null = ${myName}`);
}
