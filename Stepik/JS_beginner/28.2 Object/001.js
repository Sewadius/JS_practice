// Конструктор Object() используется для создания экземпляра объекта

const player = new Object({ score: 0 });
const gameLevel = new Object({ level: 1 });

console.log(player.score);      // 0
console.log(gameLevel.level);   // 1
