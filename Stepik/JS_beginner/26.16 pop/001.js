// Метод pop() удаляет последний элемент из массива

let arr = [1, 2, 3, 4];
arr.pop();

console.log(arr);           // [ 1, 2, 3 ]
console.log(arr.pop());     // 3
console.log(arr);           // [ 1, 2 ]
